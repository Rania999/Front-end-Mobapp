import {Dimensions, StyleSheet} from "react-native";

const styles = StyleSheet.create({
    container : {
        margin : 20,
    },
    filterButton : {
        color : "orange"
    }

});

    export default styles;